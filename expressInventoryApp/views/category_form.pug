extends layout 

block content 

    h1 #{title}

    form(method="POST")
      div
        label(for='name') Category: 
        input(type="text" name="name" required value=(undefined=== category ? "" : category.name))
      button(type="submit") Submit 

    if errors 
      ul 
        for error in errors 
          li!= error.msg 